# ClaimProof AI Configuration (Track: PS02)

# Optional Gemini API Key for narrative explanations and LLM clause evaluation
# If not provided, ClaimProof AI operates in 100% deterministic mode using
# SHA-256 reproducible vector embeddings, regex extraction, and rule evaluation.
GEMINI_API_KEY=

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
